import React from 'react';
// import PDF from 'react-pdf-js';
import NoSSR from 'react-no-ssr';
export default () => (
  <div>
         hi
  {/* <PDF file={props.file} onDocumentComplete={props.onDocumentComplete} onPageComplete={props.onPageComplete} page={props.page} /> */}
  </div>
  
);
